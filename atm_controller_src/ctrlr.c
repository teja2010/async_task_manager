/* A simple controller, which just provides a simple assignment and reports stats
 * Others can build controllers which are smarter.
 */
#include "include/atm.h"
#include "include/atm_control.h"

int main(int argc, char *argv[])
{
	//1. connect to all managers

	while(1) {
		// for each epoch/event
		//1. collect information from all managers
		//2. send out assignments
	}
}
